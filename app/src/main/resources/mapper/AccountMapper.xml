<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.mapper.AccountMapper">
  <resultMap id="accountWithRolesResultMap" type="com.example.app.dto.AccountDto">
    <id property="id" column="id"/>
    <result property="name" column="name"/>
    <result property="email" column="email"/>
    <result property="password" column="password"/>
    <result property="createdAt" column="created_at"/>
    <result property="updatedAt" column="updated_at"/>
    <collection property="roles" ofType="string">
      <result column="role_name"/>
    </collection>
  </resultMap>

  <select id="findByEmail" resultMap="accountWithRolesResultMap">
    SELECT
      a.id,
      a.name,
      a.email,
      a.password,
      a.created_at,
      a.updated_at,
      r.name AS role_name
    FROM
      accounts a
    LEFT JOIN account_roles ar
      ON ar.account_id = a.id
    LEFT JOIN roles r
      ON r.id = ar.role_id
    WHERE
      a.email = #{email}
  </select>

</mapper>